{{- if .Values.mcp.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart.resourceNamePrefix" . }}mcp
  labels:
    app.kubernetes.io/component: mcp
    {{- include "chart.labels" . | nindent 4 }}
  {{- with .Values.mcp.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- $serviceType := default "ClusterIP" .Values.mcp.service.type }}
  {{- if eq $serviceType "LoadBalancer" }}
  allocateLoadBalancerNodePorts: {{ .Values.mcp.service.allocateLoadBalancerNodePorts }}
  {{- end }}
  type: {{ $serviceType }}
  ports:
    {{- toYaml .Values.mcp.service.ports | nindent 4 }}
  selector:
    app.kubernetes.io/component: mcp
    {{- include "chart.selectorLabels" . | nindent 4 }}
{{- end }}
